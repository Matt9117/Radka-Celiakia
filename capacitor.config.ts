
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.radka.scanner',
  appName: 'Radka Scanner',
  webDir: 'dist',
  android: { allowMixedContent: false }
};

export default config;
